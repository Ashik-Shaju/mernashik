# Example .env for Book Inventory API
# Copy to .env and replace placeholder values before running

# MongoDB Atlas connection string (replace <username>, <password>, and cluster host)
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.abcd.mongodb.net/ashik?retryWrites=true&w=majority

# Application port (optional)
PORT=5000
